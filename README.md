# Unimax Production

Ready-to-run React + Vite project.

1. Import this ZIP into Replit.
2. Let dependencies install.
3. Run `npm run dev` if Replit does not start automatically.
4. Optional: add your own `public/videos/showreel.mp4` for the local showreel.

Portfolio YouTube videos are embedded directly from YouTube.
